{
  "name": "auth",
  "version": "1.0.0",
  "description": "Authentication for GeoHunt.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.13.4",
    "node-twitter-api": "^1.7.0"
  }
}
